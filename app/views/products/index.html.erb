<h3>Wellcome Admin</h3>

<table>
  <tr>
    <th>Product Name</th><th></th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td><td></td>
    <td><%= product.price %></td><td></td>
    <td><%= link_to 'Show', product %></td><td></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td><td></td>
    <td><%= link_to 'Destroy',  product, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
    
    <%#= link_to "Delete", {:controller=> 'products',:action => 'delete', :id => product.id},:confirm => "Are you sure you want to delete this item?" %>
    
    </td>
  </tr>
<% end %>
</table>

<br />
 <%#unless(session[user_name] ="admin")%>
  
  <%= link_to 'New Product', new_product_path %></br></br>
 <%#end%>
<%=link_to "register for new user", new_user_path %></br></br>
<%= link_to 'Logout', logout_path, :method => :delete %></br></br>
<%#=link_to 'orders',  orders_path%></br></br>
<%#=link_to 'users list ', users_path%></br> 

